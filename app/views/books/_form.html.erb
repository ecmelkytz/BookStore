 <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="panel panel-default">
  <div class="panel-heading"><h3>Yeni Kitap Ekle</h3></div>
  <div class="panel-body">
    <div class="col-lg-5 col-lg-offset-3"> 
      <%= simple_form_for(@book, html: { class: 'form-group' }) do |f| %>
        <%= f.input :title %>
        <%= f.input :author %>
        <%= f.input :pages %>
        <%= f.input :price %>
        <%= f.input :abstract %>
        <%= f.file_field :image%>
        <br>
        <%= f.submit "Kaydet", class: "btn btn-success" %>
        <%= link_to 'Back', books_path, class: "btn btn-default" %>
      <% end %>
    </div>
  </div>
</div>  